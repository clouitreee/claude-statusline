{
  "name": "agent-statusline",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.5.2",
  "scripts": {
    "test": "bash tests/run.sh",
    "lint": "shellcheck install.sh statusline.sh examples/*.sh tests/*.sh && pnpm run typecheck",
    "typecheck": "tsc --noEmit",
    "check": "pnpm run lint && pnpm run test",
    "audit": "pnpm audit"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
